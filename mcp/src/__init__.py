"""BioSTEAM / QSDsan MCP server package.

Named ``src`` (not ``biosteam_mcp``) to avoid shadowing the ``mcp`` SDK package on
import — the same convention used by the sibling ``openlca_mcp`` server.
"""

__version__ = "0.4.1"
